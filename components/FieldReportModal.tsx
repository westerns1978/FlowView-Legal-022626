
// This component has been merged into FlowCaptureModal.tsx to unify ingestion workflows.
// It is kept as a placeholder to prevent import errors if lazily loaded, but should not be used directly.
import React from 'react';

export const FieldReportModal: React.FC<any> = () => {
    return null;
};
